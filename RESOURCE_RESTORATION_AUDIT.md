# 1.0.19 资源组恢复与迁移功能核对（2026-09-12）

## 已修复

此前 `154431d` 把资源入口改成只返回 GetLocalPlayer 的一条状态，而界面仍写着“读取全部资源组”。同时，资源重读/写入只针对本地玩家，界面依赖旧地址判断人口是否可显示。这是实际功能退化。

- 通过 Player(0..27) / GetPlayerState 枚举当前版本的全部玩家槽，包括零资源槽；不按资源数值去重。不表示有 28 个活跃玩家。
- 每批查询三个玩家的金币、木材、当前人口、人口上限、人口极限，完整读回后再发布。空槽与执行失败分别处理；失败不退回一条本地资源。
- 界面行、选择、锁定用玩家槽标识，不再用全部为零的地址；刷新也不按旧 owner 地址合并行。
- 界面与命令行显示 native 人口数值，地址显示为横线；可以对选中的非本地玩家设置/锁定资源。
- 写前验证所有输入，写入与读回在一个 helper 命令内执行，检查同一玩家及各个目标值。范围仍为金币/木材 0..10000000、人口 0..1000，与此前一致。游戏异常中断不承诺多字段回滚。
- 本地快捷操作以 GetLocalPlayer 定位，不能误用第一行。协议由 68 升至 69，应用版本仍为 1.0.19。

## 验证证据

`test_native_player_resources.py` 通过真实生产 C 分发器，在隔离的模拟游戏函数中验证 28 槽枚举、两个零资源玩家、非本地读写、同时锁定两组、本地快捷操作、完整界面刷新、人口显示、空槽、执行异常、读回不符及写前验证。不是仅用 mock 返回成功。生产 Python 的传输白名单也有覆盖。

完整回归：**1662 passed，113 subtests passed，252.58 秒**，记录在 `analysis/resource-restoration-regression.txt`。

当前真实游戏 PID 33584 的只读验证：**28 槽，本地 Player 1，首次列表 269ms，重复列表 145ms**；复核 Player 0/1/12/27 的逐行读取身份。验证禁止 `_process_memory`，未调用备用读取。未修改真实对局资源。完整值与 DLL 哈希见 `analysis/player-resources-live.json`，复现脚本为 `analysis/verify-player-resources-live.py`。

本机结果不能替代另一台电脑上的实际运行；跨电脑结论仍待用户测试。冻结包另做源码/DLL 一致性和离线启动自检，不能把该自检说成游戏功能测试。

## 其他功能核对与保留的边界

对照资源和选择入口的迁移提交，并核对当前代码与相关回归：

| 范围 | 核对结果 |
| --- | --- |
| 多选、移动、复制入口 | 仍遍历完整选择；移动使用组操作。组移动测试覆盖 1/9/12 单位；混选、身份变化、克隆原属主与背包标志有专项覆盖。没有改成只处理首个单位。 |
| 技能枚举 | 48 是快照内联容量，超过后使用扩展结果；测试覆盖 49/300/4096 项及 12 个单位扩展数据。超限/坏身份会报错，不发布不完整列表。 |
| 背包与数量 | 保留全部六槽（包括空槽）的显示、数量和批量入口；元数据缺失不能隐藏快照里已有物品。 |
| 英雄/技能/物品字段 | 保留现有字段目录和“显示未开放”选项。基础力量/敏捷可写，总力量/敏捷只读；基础智力只读，总智力可写及其备注仍保留。并非本次去掉功能。 |
| 世界操作 | 对照 `4cf95ac`，迷雾、暂停、和平等入口的变化为移除外部内存上下文，原有 native 操作及范围保留。 |
| 记住的单位 | 仍按保存身份重新绑定；失效身份或 helper 失败会显式报错，不回写复用地址。这是已有拒绝条件，未宣称永远成功。 |

**仍存在的复制例外：** C 克隆链对部分技能的整数除零异常、游戏拒绝添加技能等情况使用 `continue`，可能保留新单位但跳过该技能或等级恢复。当前成功提示不等于逐项无损复制。这是之前处理地图异常技能时留下的行为，本轮未修改这条已反复修复的执行链；不能据完整回归通过宣称所有地图技能都能完整复制。

本次确认并修复的是资源入口及相关界面的多组退化；上述核对没有发现其他同类“集合只保留首项”的情况，不代表完成所有地图和所有电脑上的穷尽验证。

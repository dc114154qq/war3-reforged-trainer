# Warcraft III Reforged Trainer 2.0.3

适配 Warcraft III `3.0.0.24268`。

## 变更

- 使用 `NtMapViewOfSection` 的手动映射经典链路，绕开 3.0 进程对远程 `LoadLibraryW` 的拒绝。
- 通过目标进程实际模块基址解析系统 API，避免固定系统 DLL 地址导致跨设备失效。
- 为手动映射桥接注册并回收 `.pdata`，记录映射、回调、线程和清理状态。
- 仅在首次 Hook 未安装、没有回调且资源已完整释放时重试同一链路；不重复执行已进入游戏的请求。
- 应用和文件版本统一更新为 `2.0.3`。

## 验证范围

- 协议和传输层单元回归。
- 冻结包运行自检和独立目录/CWD 可移植性模拟。
- 目标进程实测仍需以实际地图和选中单位状态为准。

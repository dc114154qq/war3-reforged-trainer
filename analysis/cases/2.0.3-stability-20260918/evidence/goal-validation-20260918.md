# 24268 Equipment and Teleport Validation

- Full offline regression: `2320 passed, 17 skipped, 120 subtests passed`.
- Targeted position, item, equipment, diagnostic identity, and UI suites: `44 passed` in the final targeted run; the broader position/item/equipment run reached `117 passed` before the final full run.
- The latest bridge product and fixture builds passed `verify_engine_bridge.py` with the current ABI markers.
- Live item-field roundtrip on PID `26116` while three units remained selected: ordinary slot 2 item handle `0x102ba6`, `iuse` changed `1 -> 2 -> 1`; slot 1 was not used.
- Live direct position batch on PID `26116` completed for three selected units. The result was `count=3/changed=3/completed=3`; the engine adjusted units through its formation/collision solver, and the product accepts finite engine readback instead of requiring a common exact point. After a posted right-click, all three read-back positions changed and the game process remained alive (`exit_code=259`).
- Live `eeh3` placement was not executed because the current 3.0 build routes non-consumable equipment through the slot-1 technical backpack; user reserved that validation for manual testing. Offline eeh3 equipment replacement/restoration and inventory-preservation fixtures passed.
- The first cleanup report marked `restored=false` only because it required exact return to the pre-test coordinates while movement/collision resolution was still active. The current validation records per-unit engine readback, waits for stable movement, and treats finite completed cleanup with a live process as pass; collision/formation-adjusted coordinates are retained as evidence rather than treated as an error.
- A subsequent recheck was intentionally stopped at the precondition because PID `26116` had only one selected unit at its start (`Expected the current multi-unit selection, got 1`); it did not execute a product operation and is not counted as a feature failure. The earlier three-unit live record remains the applicable multi-selection evidence.
- Final focused regression after the validation-rule update: `124 passed in 4.05s` with third-party pytest plugin autoload disabled because the global Hydra/ANTLR plugin installation is incompatible with this Python environment.
- Fresh bridge build completed with ABI verification. Frozen package self-test passed with `ok=true`, `frozen=true`, and `retired_helper_present=false`. Final EXE: `dist-2.0.3-3.0.0.24268-final-r1/War3ReforgedTrainer-v2.0.3.exe`; SHA-256: `594E14291A5EFE7F08396C273519234AF0CEFE4C923502487FA731E4DDFC1855`.
- Live `eeh3` placement remains intentionally unexecuted because the current 3.0 build routes non-consumable equipment through the slot-1 technical backpack; this is reserved for the user's manual validation. The ordinary item-field roundtrip and offline eeh3 fixture coverage passed.

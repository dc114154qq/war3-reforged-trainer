# 当前目标：Warcraft III 3.0.0.24268

用户于 2026-09-13 明确要求：先发布当前修改器为 1.0.19，再适配今日游戏更新。会话目标已重新创建并激活。

## 发布基线

- 旧游戏已验证构建：2.0.4.23745。
- 已发布包：War3ReforgedTrainer-v1.0.19-4124cd5.exe，源码 4124cd582ed624eeb85d665484755fa8cae65697。
- 发行页：https://github.com/dc114154qq/war3-reforged-trainer/releases/tag/v1.0.19
- SHA256：b90c4da38c3c06116a9bb8d7e6df26f270fb0beecfb6be919092112c2f6ebe94。
- 原标签及旧附件保留；发行说明明确本次 EXE 对应提交。新适配不混入旧发布包。

## 目标与验收

适配分支 `codex/war3-3.0.0.24268-adaptation`。本机 Warcraft III.exe 文件版本及 .build.info 均确认 3.0.0.24268。

用户补充：新版编队上限为 24，已在游戏选中超过 12 个单位。读取、显示、移动、复制及其他批量操作均需完整支持 24 个单位，不能截断为 12。

在运行受支持游戏构建的不同电脑上，完整保留资源组、多选、英雄、技能、物品、复制和大象功能，快速、稳定、准确。跨设备通过启动时的版本、模块和运行时布局识别建立快速缓存；后续操作使用缓存和定向访问，不把备用读取、重复扫描或全进程扫描作为产品链路。参考经典版链路，但以功能和实测结果为准，不以删减功能换取兼容。

先做必要的二进制比较和源码审计，再做针对性的真实 C/native 测试与本机游戏测试。构建指纹、函数调用约定、对象布局需要证据，不直接放行新构建或沿用旧地址。研究时读取指定游戏模块生成离线证据，与产品运行时备用扫描不是一回事。

打包前完成必要回归、包内一致性和启动自检，保存可反馈日志，给出独立 EXE 路径后等待用户测试。不频繁要求用户测试，不重复无关全量检查。明确代码、本机和跨电脑验证的界限；跨电脑尚未实测或异常技能可能跳过时不能宣称全部完成。

## 进度

- 已完成：1.0.19 正式发布、服务端 EXE 哈希校验、新游戏版本确认、新适配分支。
- 已完成：3.0 经典选择链路适配。玩家对象内的 `root/count` 链表首次连接动态绑定并缓存，后续沿缓存链表读取并逐项校验；产品路径支持最多 24 个单位。旧 bootstrap/profile 与备用读取均不进入 3.0 产品链路。
- 新游戏磁盘 SHA256：bd2a0dc256289de45287bb60f3725b88f1235216d5177984377fe1ca22840a12；时间戳 0x6aa4de70、映像大小 0xe155000，详见 analysis/game-image-3.0.0.24268.json。
- 已对当前游戏 PID 23864 的指定模块头和 .text 做只读采集，无注入或游戏状态写入。4085 个页面不能从进程外读取；原始证据只留本机，缺口明确记录，不作为可调用代码依据。
- 已完成：当前构建的 native 注册表、线程上下文、对象索引和桥接调用约定已通过 3.0.0.24268 指纹校验；产品桥接 DLL 使用模块基址和对象表索引，不使用机器固定地址。
- 已完成：3.0 版本桥接 ABI、24 单位容量、显示与批量入口已通过当前回归测试和本机真实进程烟测；独立 EXE 已打包并通过冻结包启动自检。
- 已补齐 3.0 主模块共享 Section 的只读采集并定位 Fiber/TLS 异常分发路径；旧注册锚点仍未恢复，继续离线重建，不能沿用旧 profile。
- 已取得主模块共享 `.data` 的只读目录证据；发现大量 `.rdata` 表引用，但 native handler 仍需 3.0 的索引/解码上下文，未将任意地址接入产品。
- 3.0 主路径实机只读验证：动态发现 28 个玩家对象，筛选出 3 个唯一选择列表，其中主列表 14 个单位、14/14 身份映射通过；首次建立约 29 秒，缓存链路后续读取约 0.01 秒。证据见 `analysis/native-bootstrap-24268/classic-selection-live.json`。
- 已完成：3.0 当前局已验证资源属性缓存读写、28 个资源组枚举、英雄组件字段读写、物品槽数组与 item wrapper/rawcode 读取、能力实例枚举、技能批量查询/修改路由、物品批量路由、复制技能与物品路由，以及最多 24 单位的产品测试入口。
- 已完成：`Atrg`、`Bhea` 等引擎保留组件已从复制添加列表中识别并跳过，避免把不可添加的内部组件误报为普通技能。
- 保留缺口：第二台实体电脑的跨设备运行、破坏性写入的完整用户验收和 `world_effect` 实机链路仍需单独验证；这些不以代码测试结果代替。

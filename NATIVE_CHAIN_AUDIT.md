# 1.0.19 后续分支：native 链路源码审计

审计日期：2026-09-06。起始提交：`946d24a`，后续修正包括 `dd44267`、`b21407d` 和协议 26 技能扩展，分支：`codex/v1.0.19-followup`。
本记录基于源码和离线测试；未打开修改器 EXE、未连接游戏、未注入 DLL。
正式 1.0.19 和现有测试包未覆盖。此前用户确认当前操作很快，但这不能证明下面所有架构要求已经完成。

## 本次修正

- `_selected_candidates_snapshot` 删除空快照进入旧 JASS 映射、全局单位索引及强制刷新索引的路径。空结果不再依赖 `_persistent_native_initialized` 标志判断是否允许扫描；重连时标志变化也不会打开旧路径。
- 整组选中单位校验 JASS 句柄、完整对象句柄、owner、unit，拒绝缺失、重复、映射不一致和超限结果。全部通过后才更新 owner 索引；失败不会返回部分组或写入部分映射。
- `read_unit`、`read_unit_fields`、`read_unit_native_selection` 删除报错后的自动全局候选扫描。错误直接传播，同时安排清空旧字段和旧候选列表。显式的手动候选诊断入口仍存在，尚不能声称整个产品没有旧扫描功能。

## 目标与当前证据

| 要求 | 源码证据 | 当前判断 / 待完成工作 |
| --- | --- | --- |
| 进程内 DLL 在游戏线程读取当前选择 | C `war3_persistent_selected_snapshot` 使用 CreateGroup / GroupEnumUnitsSelected / FirstOfGroup，并调用单位和 agent resolver 校验对象关系；由 hook 命令分发执行 | 主选择路径已经具备；不同地图阶段和 Windows 环境的实际稳定性未由本次测试证明 |
| 首次读取不依赖全进程扫描和特定堆布局 | Python `persistent_native_init` → `_discover_native_handlers_near_table` → `_find_native_table_regions`，后者搜索私有内存中的 UnitAddAbility 字符串，限制单区域 2 MiB；随后搜索表附近 ±0x80000 | **未完成**。表位置发现仍依赖堆搜索和邻域假设，发现失败后的后台初始化重试也会再次搜索。下一步应从游戏模块内的注册代码/表访问函数定位入口，再由 DLL 按真实表结构解析 native；不能仅预热或扩大扫描范围 |
| 切换到新单位不扫描全局单位列表、不错误锁定旧单位 | `_selected_candidates_snapshot` 每次消费 native 选择及完整身份；本次删除旧映射回退和界面失败后的自动扫描 | 该调用链已收敛。其他详细字段路径仍有基于全局初始化标志的分支，不能把此局部证据推广到所有读写入口 |
| 英雄、非英雄、物品、数量、技能字段完整 | C `War3PersistentSnapshot` 有基础值、英雄属性、六个物品槽及数量；协议 26 把第 49 项起的技能以可变长度追加传输，Python 校验并还原完整返回列表 | 固定 48 项传输容量已消除。枚举前 256 索引保留原稀疏探测，随后遇到首个空句柄结束；最多接受 4096 索引，检测到第 4097 项则报错，保护游戏线程。假 native 边界和多单位验证已通过；真实引擎的枚举结束语义以及 UI 全部字段迁移仍未证明，整体目标仍未完成 |
| 所有详细字段由进程内对象/函数可靠提供 | `_unit_fields_from_candidate` 仍主要按跨进程地址和组件偏移读值；`_ability_instances_from_candidate` 使用邻域对象查找；英雄智力查询失败仍可展示旧候选值。当前 native 候选已直接绑定不可变快照，物品/摘要按完整身份消费，协议 27 已添加针对绑定单位的快照刷新 | **未完成**。跨批次快照混用和绑定单位物品替换后的旧快照回读已修正；仍需补齐 DLL 字段及可写能力、跨 UI 操作持有绑定身份。缺失字段不能用猜测值冒充有效结果 |
| 单次操作高效执行整组单位 | `move_selected_group_to_mouse` → op 132 → `war3_move_selected_group_at_point` 在一次 hook 回调内先收集、后移动；`create_local_units` 仍循环调用单次 `create_local_unit`，每次克隆提交 14 个操作描述项 | 群移已实现一次回调；复制尚非整批一次回调。克隆后续重构必须保留英雄、技能、物品及所有者语义，并定义部分创建失败的结果，不能只改变循环位置而漏字段 |
| 无普通/备用读取依赖 | 主选择路径使用持久快照；源码仍有 `BackupReadWar3Trainer`、手动候选列表、诊断 selection-manager 入口及详细字段回退 | 主路径与整个产品需分别核对。旧入口仍存在，不得声称全部清除；迁移时核查 UI、CLI、写入回读、锁定任务的所有调用者 |
| 不同游戏状态、堆布局和 Windows 版本稳定 | 现有测试包括 C 假 native 群移、Python 协议与路由验证；本次只做离线验证 | **未证明**。离线测试不能替代真实游戏版本/载入/新建单位/单位删除和重建/重连/Windows 环境验证。游戏 EXE 测试由用户按其安排进行，不逐改动要求测试 |

## 本次验证范围

最终运行 `python -m pytest -q`：147 passed，27 subtests passed；`git diff --check` 通过。这里的失败场景是测试主动注入的异常，并非新发现的实机故障。不能以“会拒绝失效快照”代替正常选中新单位时应稳定成功的要求。

`test_native_helper_runtime_features.py` 新增空快照（初始化前后、显式快照和实时查询）、换新单位、四类缺失身份、重复身份、映射不一致、超限组验证。失败必须不调用全局索引、不重新查询旧选择、不发布部分 owner 映射。

`test_native_read_failure_routing.py` 编译并执行实际 GUI 嵌套回调，但用 mock 替代 trainer 和 Tk 调度器。覆盖连接失败、读取失败和成功读取，检查错误对象原样传播、清空旧候选、正常结果仍展示，以及不调用候选扫描。不会启动 GUI 或游戏。

这些测试证明本次修正的路由与校验行为，不证明实机速度、对象偏移兼容性或完整 native 字段目标已完成。

## 协议 26 技能扩展

保留每单位 140 个 uint64 的固定快照，前 48 项技能仍在原位置。全部单位固定快照之后，按照相同单位顺序追加超出 48 项的 `(rawcode, level)` 对，数量由各快照的 `ability_count` 确定。完整对象身份、物品槽和英雄字段偏移保持一致。扩展缓冲区按需增长，最终拼接完成后才发布结果；分配或 native 调用异常不会发布部分数据。协议号已同时更新 Python/C，DLL 已重编译；应用版本仍为 1.0.19，未打包 EXE。

`test_native_snapshot_payload.py` 编译实际 C helper 源码，用假 native 调用完整快照函数，再由实际 Python 解析器解码。覆盖 0/48/49/256/300/4096 技能、英雄与非英雄混合组、每单位物品数量、12 单位最大载荷、旧范围空洞、13 单位和 4097 技能超限、扩展分配后的 SEH 异常、逐个 HeapAlloc/HeapReAlloc 失败、截断/多余结果和非法计数。分配跟踪确认每条路径均释放自有内存，失败返回空载荷；group 创建后均被释放。

本次的 C 测试不调用游戏、不安装 hook，也不验证真实游戏中第 256 索引后的空句柄是否一定代表列表结束。该语义仍需结合可反汇编的引擎实现确认。前一次磁盘 EXE 分析只找到 native 名称字符串，未找到有效注册代码引用，不能据此宣称已定位冷启动入口。

协议 26 最终全套离线验证：`python -m pytest -q --tb=short` 返回 156 passed、41 subtests passed；`git diff --check` 通过。C helper 以 `clang -shared -O2 -Wno-microsoft-goto` 编译并链接 user32/kernel32，生成分支内 DLL；正式发布 EXE 未改动。

## 单位候选绑定本次快照

`UnitCandidate.native_snapshot` 保存本次选择返回的 frozen dataclass；批量选择映射及单单位大象入口都附上快照。摘要消费该对象内的 HP/MP、坐标、技能数、物品和英雄标志，不再把 `_last_persistent_native_snapshots` 中按地址匹配的另一批数据拼入结果，也不再为摘要额外跨进程读取 HP/MP。消费前核对完整 handle/owner/unit，摘要额外核对 JASS handle。该附加字段不参与候选相等性比较或 repr。

物品对象定位优先使用绑定快照；手动候选若借用最近一批快照，必须完整 handle/owner/unit 均匹配。物品数量用于写入后回读时，从已通过 slot/full-item-handle/type 校验的物品地址读取实时 i32，不再把写入前的快照数量当回读结果。无法读取槽位不再被当作有效空槽；有 native 载荷的槽位验证失败即返回无结果，不受并发重连时初始化标志的变化影响，也不退回全局物品搜索。

离线测试使用真实选择映射、摘要、物品读取和数量写入回读方法：一个线程暂停在映射后，另一个线程替换同地址不同代对象的全局快照，原摘要保持原批次数据；另测相同身份不同批次、缺失/错误绑定、旧手动候选、数量 3→7 的真实回读、重连时物品变动和不可读空槽。上述对象内存和写入 native 均由 mock 提供。

绑定修正当时的剩余限制：没有把全部详细字段读写迁入 DLL，也没有实现替换物品后的定向刷新；该定向刷新随后在协议 27 中实现，见下节。真实游戏内的并发选择、物品生成/删除和读写成功率仍由后续实机验证确认。

绑定修正最终离线验证：164 passed、44 subtests passed；源码差异检查通过。应用版本 1.0.19、helper 协议 26，本轮未改 DLL、未打包 EXE。

## 协议 27：针对绑定单位刷新快照

新增 op 133，命令携带 JASS 句柄、预期 unit 地址、完整对象句柄和 owner 地址。DLL 通过单位 resolver 和 agent 对象表校验全部关系，在任何字段 native 调用前拒绝不一致；然后复用原快照字段采集和技能扩展输出。定向操作不创建 group、不枚举当前选择，也不改变选择。Python 使用共用解码器并再次检查返回身份，只生成绑定新快照的候选，不覆盖全局当前选择快照。

`_write_inventory_slot_field` 在替换前刷新绑定目标，替换后立即再刷新同一目标，检查六槽结果和新 rawcode。native 路径取消原有六次旧快照重试及 30/80ms 固定等待；返回字段的可写地址指向新物品对象。缺少完整六槽结果时不按空背包继续写入。`read_unit_fields_by_identity` 在已有匹配 native 身份时也定向刷新，让随后的界面回读得到新物品信息。

离线测试补充：真实 C 定向采集第二个单位（含 300 技能）时选择枚举次数为零；六类身份故障在字段调用前失败。Python 实际命令白名单及二进制打包覆盖 op 133，实际物品替换函数在旧对象释放后通过两次定向快照得到新对象，固定 sleep/选择查询/全局物品扫描均被禁止。手动身份回读复用匹配 JASS 身份，且定向结果不改变全局选择快照。DLL 重编译，Python/C 协议均为 27；应用版本仍 1.0.19，未打包或启动 EXE。

剩余限制：只有持有本次 native 快照，或最近选择快照仍有完整匹配身份的候选能走该定向路由。UI 只存完整对象身份、未长期持有 JASS 绑定的手动候选，若最近选择已变化，仍可能无法命中此路由；此时原有诊断路径尚存，不能声称所有手动/锁定操作都已迁移。首次初始化的堆搜索、详细字段完整 native 化及真实引擎枚举语义仍待解决。

协议 27 的历史离线验证：171 passed、50 subtests passed；这些验证没有启动游戏，也不证明真实游戏中所有字段已经稳定可写。当前分支已推进到协议 28，DLL 与 Python 协议一致性需以当前源码和重新构建产物为准。

## 公共当前单位定位入口统一

审计发现 `write_selected_unit_fields`、`set_selected_unit`、`locate_current_selected_unit` 和旧 CLI 包装 `locate_selected_unit` 仍调用 `locate_selected_unit_by_handle`。该入口原先依次读 selection manager、已知 unit 指针、固定/历史句柄槽，并按 80/200/450/900/1600ms 间隔重试；开启 `allow_deep_scan` 时还会搜索选择句柄地址。它不因读取按钮已经使用 native 而自动迁移。

现已删除该入口的旧定位逻辑，统一消费 `_selected_candidates_snapshot` 返回的当前 DLL 选择、完整身份和不可变快照绑定。为旧调用方保留 `allow_panel_fallback` / `allow_deep_scan` 参数，但两者均不能再启用旧槽或扫描。调用方提供的内存会话仍由调用方拥有；自行打开的会话由 context manager 在成功/失败时关闭。预热入口也删除了异常后重新探测旧选择布局并重试的分支。

新增 `test_native_selected_write_routing.py` 直接运行公共方法，覆盖新单位切换但面板输入相同、两个兼容开关的全部组合、会话所有权、空选择不得写旧目标、整组身份校验失败不得部分写入、当前读回及预热失败。并从真实 GUI 源码提取 `apply_locks_once` 回调执行，确认未固定身份的单位锁每次调用都会走当前 native 选择。旧定位函数、全局索引和固定 sleep 在这些测试中均设置为一旦调用即失败。

范围边界：修改的是公共当前单位定位入口及其预热回退；明确按身份固定的锁仍走身份写入，旧候选诊断和独立备用入口尚存。基础字段的实际值写入仍多为跨进程内存写入，本次没有将其宣称为全部游戏线程 native 修改。原生冷启动定位、完整字段迁移、UI 长期持有 native 身份的工作仍待完成。协议 28 新增当前生命/魔法值的 `SetUnitState` 游戏线程写入，并加入最大生命/魔法值 setter 的游戏线程调用；回复率仍保留地址写入路径。该部分只完成离线审计，未启动或打包 EXE，应用版本仍为 1.0.19。

## 协议 28：当前生命/魔法值写入

基础属性写入现在会在已验证的单位句柄上调用游戏内 `SetUnitState`：生命使用状态 0，魔法使用状态 2；最大生命/魔法值调用已由复制单位链路验证过的 `BlzSetUnitMaxHP/BlzSetUnitMaxMana`。调用由 native helper 的游戏线程回调执行，并对句柄和值范围做校验。回复率仍未找到同等可靠的游戏线程 setter，因此仍是跨进程地址写入，不能把本节描述为全部基础属性已经迁移。

公共定位迁移最终离线验证：`python -m pytest -q --tb=short` 返回 179 passed、54 subtests passed；`git diff --check` 通过。用户提供的 `War3ReforgedTrainer-v1.0.18-Capstone.exe` 是历史构建，未包含本分支后续提交；当前源码与协议 28 DLL 尚未打包成新的 EXE。


## 基础属性写入复审：纠正句柄类型与测试盲区

复审基线 `3defc7d` 发现：`_write_basic_unit_values_to_candidate` 把 `candidate.handle`（完整对象身份）传入 JASS setter；正确参数应为绑定快照的 `handle`（JASS 句柄）。此前 179 项测试没有直接覆盖该路径，且生产代码依据测试对象是否存在 `_native_handlers` 退回地址写入，因此测试通过不足以证明新 setter 链路有效。

本次修正：按完整身份取得 JASS 句柄，先通过 op 133 定向刷新校验；将生命、魔法、最大值及坐标 setter 合并为一次命令，命令头使用 JASS 句柄；写入后再次定向读取并返回新的候选对象。移除依据 `_native_handlers` 或 `_persistent_bootstrap_lock` 成员是否存在的测试专用回退。未绑定的手动身份只能从完全匹配的 native 选择取得句柄，不会改写其他选中单位。只改一轴时使用定向快照中的另一轴，不再用零补缺失坐标。提高当前生命/魔法超过旧上限时也走 native 最大值 setter。

数值校验遵循已有 DLL 实际范围：当前生命/魔法为 ±100000000，整数最大值不超过 1000000000，坐标为 ±1000000；超界请求在写入前拒绝，不再因测试回退而宣称支持任意 float32。最大值沿用整数 setter，自动抬升上限采用向上取整。回复率仍使用属性地址写入，不是 native 失败后的备用路线。

新增离线测试直接调用实际基础属性函数和真实命令白名单/序列化，覆盖两种句柄不同、无属性地址、同时修改多字段、只改一个坐标、身份失效、未绑定身份不匹配、非法参数、native 失败不得回退以及写后返回值刷新。原大数值测试改为通过模拟 native 命令读写受支持的大值，不再依赖生产代码中的测试特判。

仍未证明的范围：定向校验和 setter 命令属于不同回调，不能据此宣称已消除期间单位销毁/句柄复用的竞争；命令中多个 setter 异常时没有整体回滚。详细字段表的通用写入入口仍需单独迁移，冷启动 native 表发现、能力实例邻域扫描、完整字段语义和跨进程生命周期缓存仍待处理。源码还显示 native strength/agility 使用 include-bonuses=1，而详细表此前将它们标为基础值；英雄等级此前还关联了未经本次验证的写入偏移，均应作为后续修正项。当前并未达到完整目标，不打包或启动 EXE。


## 协议 29：英雄基础三维与总值分离（2026-09-07）

协议 28 的 strength/agility/intelligence 来自 GetHeroStr/GetHeroAgi/GetHeroInt(unit, 1)，含装备等加成。之前将 strength/agility 当成基础字段展示存在语义错误；英雄等级还被赋予了未经验证的 data+0xF8 写入地址。

现在 DLL 在同一次采集中额外调用上述三个 getter(unit, 0)，将基础三维追加至固定快照末尾的 qword 140..142。每单位固定头由 140 增为 143 qword；已有身份和技能槽偏移保留，超过 48 项技能的扩展区仍位于所有固定头之后。Python/C 协议同时提升为 29；三维数值按有符号 int32 解码。旧固定头长度不再被当成完整的新快照。

详细字段使用基础字段的新值，同时单独展示力量、敏捷总值；智力总值也直接使用绑定快照，不再在 native 分支查询失败后使用旧候选值。缺少外部英雄组件时仍可展示这些 native 字段，但没有对应写入地址。英雄等级与新增力量/敏捷总值只读，不再虚构等级偏移。已有独立设置英雄等级功能未改动。基础力量/敏捷及经验的通用字段写入仍有地址路径，尚未全部迁至 native setter；不能将本次字段读取修正宣称为完整写入迁移。

离线验证包含真实 C 假 native 的基础值 10/总值 25 区分、混合英雄和非英雄组、48 项边界和长技能列表、定向快照，以及 Python 面板缺少组件、总值只读、等级地址写入被拒绝、负数三维及旧数据长度拒绝。DLL 已重新编译，SHA256 为 620004AEB6B76FDAD5EF69D340EF10F0AACB0CEDCD46AB8B19E2010AEEDFFB50。应用版本仍为 1.0.19；没有启动或打包 EXE，实机兼容性尚未验证。


## 协议 30：写入回调内校验完整身份（2026-09-07）

协议 29 的基础属性操作虽然先做了定向快照验证，但 setter 在下一次回调才执行，期间单位销毁或 JASS 句柄被复用仍可能改变目标。本次新增 op 136，将预期单位指针、完整对象句柄和所有者指针随写入命令发送；命令头仍为 JASS 句柄。DLL 在同一游戏线程回调内使用已注册的单位和 agent 解析器验证对象表关系，并检查对象句柄、所有者类型标签、所有者句柄及反向单位指针。

基础生命/魔法/上限/坐标写入批次以 op 136 开头，之后每个 setter 前都重新校验，防止前一个 setter 触发游戏脚本、销毁对象后继续执行。op 134/135 必须有此校验头，否则直接拒绝。此校验批次只允许这两个属性 setter 和位置 op 94；定向快照也复用同一个身份校验函数。协议同时提升至 30，快照固定头仍为 143 qword。

新增 test_native_identity_guard.py 在隔离临时目录中运行真实 C run_command 分发器，使用假对象表和 setter，不连接 Warcraft。覆盖正确 JASS 参数、空解析结果、对象替换、所有者不匹配、完整句柄变化、反向指针失效、解析器缺失、校验头缺失或放错位置、解析异常、类型标签不匹配，以及第一个 setter 后对象代次变化。最后一种情况确认只执行第一个 setter，后续调用停止。Python 测试同时检查 guard 参数、真实白名单和二进制打包。

完整离线验证：198 passed、66 subtests passed；git diff --check 通过。DLL 重新编译，SHA256：A1A0BF99B4BEC1CE7D1296223CA288F4912F96C719C7F6FBBF347F552EA8623D。应用仍为 1.0.19，未打包或启动 EXE。

边界：此次覆盖基础属性按钮的 native 批次，独立移动入口、技能/物品修改和通用字段写入尚未全部接入 op 136。回复率仍在外部按地址写入。本批不提供事务回滚，已完成的前序 setter 不会撤销；不能据此宣称所有操作均已消除身份竞争。冷启动定位和字段迁移等完整目标仍未完成。


## 字段表基础属性写入统一（2026-09-07）

复审发现字段表和字段锁调用 `_write_unit_fields_to_candidate`，原先 hp_current/hp_max/mp_current/mp_max/x/y 会落到 `_write_memory_value`，绕过基础按钮的 native setter 和 op 136 身份校验。本次六个字段统一映射到 `_write_basic_unit_values_to_candidate`，同一请求中的基础字段合并成一次受校验的 native 写入批次，并使用写后定向快照返回实际值。

`UnitMemoryField` 新增显式 native 写入能力，避免依赖非零 write_address 来标记这些字段可写。来自 native 快照的这六项可在外部属性地址缺失时操作，write_address/write_type 保持空；UI 写入按钮和字段锁已有的 writable 判断因此仍适用。没有把不可写的英雄等级或其他总值字段开放为地址写入。

写入前先解析全部字段、检查存在性和写入能力，并验证基础字段输入的 float32 数值；重复指向同一基础字段的 key/标签请求直接拒绝，避免合并时静默覆盖。混合请求先提交基础字段批次，再执行其他字段，结果按输入顺序返回；仍不提供整批事务回滚，也不能保证其他字段的运行时失败撤销先前写入。其他字段中的地址路径未在本次迁移。

新增测试实际调用公开 write_selected_unit_fields、字段构建器、基础写入函数，覆盖无属性地址、中文标签、六字段合批、guard/JASS 参数、实际读回值、无效混合请求、重复字段、native 失败不得写旧地址。完整离线结果：205 passed、66 subtests passed；git diff --check 通过。协议保持 30，DLL 无改动。应用仍为 1.0.19，未启动或打包 EXE。


## 新单位映射不再以外部生命属性块为前提（2026-09-07）

此前 `_selected_candidates_snapshot` 和 `_elephant_selected_candidate` 经 `_candidate_from_identity` 映射，而后者要求 `_candidate_from_owner` 找到可读的生命属性块。即使 DLL 已返回完整有效快照，属性元数据缺失也会被当成单位无效，阻断 native 字段读写。

现在两入口通过 `_candidate_from_native_snapshot` 映射：旧属性元数据可用时保留；不可用或读取失败时再次明确检查 owner 类型标签、完整对象句柄、owner->unit 及 unit->handle。身份关系一致才构造绑定 native 快照的候选，外部属性地址置零，不猜偏移、不扫描补齐。身份不可读、代次变化或对象关系不匹配仍拒绝；全组选中映射成功后才发布索引。

界面按完整身份调用的 read_unit_fields_by_identity / write_unit_field_by_identity / set_unit_by_identity 也可恢复与该身份完全匹配的 native 快照，再进行同样映射，不会把上一代对象或另一个单位的快照套过来。没有匹配快照的旧手动身份继续保持原有校验，不能承诺脱离 native 身份记录时任意历史单位都能操作。

新增测试运行实际单位身份映射、公开选中读取、公开字段修改和按显示身份修改，覆盖生命属性缺失/不可读、零属性地址下 native 读写、四种身份关系损坏、身份不可读、组内第二单位失效不得部分发布，以及地址复用不得写入新代次。没有连接游戏或启动 EXE。此改动只解除 native 操作对外部生命元数据的依赖，其他外部字段缺失并未因此自动补全，技能实例邻域扫描和冷启动函数发现仍待迁移。


## 技能列表直接消费 DLL 枚举（2026-09-07）

此前详细字段表只在检测到英雄组件后调用能力包装器邻域扫描，非英雄没有实例列表；即使持久快照已经包含完整 ability_ids/ability_levels，字段表也没有使用它们。缓存或堆布局变化可能导致漏显示。

现 native 分支按绑定快照顺序生成每个能力的 ID 和等级字段，适用于英雄、非英雄和外部组件缺失的情况；包含超过固定 48 槽的扩展项。列表读取不再调用 `_ability_instances_from_candidate`，不会用邻域包装器/历史实例缓存决定完整性。ID 与等级长度不一致时明确报错，不能 zip 静默截断。字段是只读原生枚举结果，地址保持零，不伪造实例对象地址。

英雄可学技能配置仍作为独立组件字段保留；native 读取时不再补充无法从快照证明的包装器效果类/虚表/缓存地址。已有技能修改和效果类查询入口未改变，相关操作内部的邻域解析仍待迁移，不能将此变更描述为所有能力路径均已消除扫描。

新增测试覆盖英雄/非英雄/无组件、0/1/49/300 项列表、并发替换全局快照不污染绑定结果、增删技能后刷新、键唯一、长度异常拒绝、只读字段不允许按地址修改。未启动或打包 EXE，版本仍为 1.0.19，协议与 DLL 保持 30。


## 物品显示与外部背包元数据解耦（2026-09-07）

字段表原先直接消费 `_inventory_items_from_candidate`：若找不到外部 inventory 组件或槽位记录，即使 DLL 快照有物品，表中也不显示。本次 native 字段表按绑定快照生成六个槽位 ID 和数量，包含空槽和大于 999 的数量；ID、句柄占用状态不一致或数组长度不足六项时报错，不把数据损坏当成空背包。

外部元数据仅用于保留已有写入权限：匹配槽位、rawcode 和对象地址的已解析信息可继续供原有物品 setter 使用，显示值仍取自快照。缺失或不匹配的元数据不提供写入地址，物品及数量只读展示。无 inventory 组件时完全不调用外部物品解析。此变更没有把只读显示冒充完整物品写入迁移；背包写入对外部元数据的依赖及物品生命周期校验仍需后续处理。

新增离线测试覆盖英雄/非英雄、有无组件、首尾占用和空槽、大数量、并发最新快照替换、有效元数据保留权限、对象/ID 不匹配不开放写入，以及不完整快照拒绝。直接物品写入后的实时数量验证路径保持原样，未改成拿写前快照验证修改。应用仍为 1.0.19，协议保持 30，未启动或打包 EXE。


## 协议 31：绑定物品数量写入（2026-09-07）

新增 op 137，跟随 op 136 单位身份校验执行。通过已注册的 UnitItemInSlot、物品解析器核对槽位、JASS 物品句柄和对象地址，再调用 SetItemCharges；同一回调内重新检查单位身份、槽位、物品对象及本回调记录的完整物品句柄，使用 GetItemCharges 立即读回。缺失 handler、物品被替换、身份变化或读回不一致均失败，不回退外部地址写入，也不自动重试 setter。

Python 写入前定向刷新并比对原槽位的句柄/地址/ID，有效 native 物品数量不再依赖外部背包组件、数量偏移或 50ms 等待。数量范围使用既有 i32 转换；native 路径支持大于 999 的非负数量，旧路径上限不变。native 物品有句柄和对象地址时，字段表数量项按 native 能力开放，写入地址保持零；物品类型替换尚未迁移。

真实 C 分发器测试覆盖 1500 数量成功、槽位替换、物品解析失败、getter 缺失、setter 后槽位/对象代次/单位变化及错误读回；Python 覆盖无外部组件、无固定等待和刷新后换物品不得继续写入。完整回归一次：244 passed、73 subtests passed。DLL SHA256：88B462AAD2B025945C31AFBF3842C88A9774D54F3F77E874E696F330C8B9CF8E；应用仍 1.0.19，协议 31，未启动或打包 EXE。

边界：尚未向快照传输完整物品代次，回调前若 JASS 句柄和对象地址同时复用为同 ID 的物品，仍需更强的跨回调代次绑定。本次也没有实现 setter 回滚、全部物品操作迁移或冷启动无扫描，完整目标仍未完成。


## 协议 32：跨回调绑定完整物品身份（2026-09-07）

快照固定头扩展为 149 qword，追加六个 item_full_handles（物品对象 +0x18 的完整身份）。数量写入在定向刷新时对比此身份，op 137 后附 op 138 身份描述，由 DLL 在 setter 前核对；缺描述或代次不符即拒绝。已有数量回调内的写后身份及数量验证保留。字段表外部元数据也要求完整物品身份匹配才赋予地址写入能力。

测试覆盖实际 C 快照传输、缺失描述拒绝、同句柄/同地址/同 ID 但代次不同的对象在 Python 和 C 两侧均不得写入。完整离线结果：245 passed、75 subtests passed；DLL SHA256：274503DB431D7144FE5DA7A0B82C3E938B6F3B04CDD9AC79E50F35E3C74190A8。未启动或打包 EXE，应用仍 1.0.19。物品替换等其他操作和冷启动定位仍待迁移，本次不表示完整目标完成。

## 冷启动注册路径的离线证据（2026-09-08）

本轮使用前一轮保存的主模块 `.text` 与本机磁盘 PE 做离线分析，没有重新读取游戏进程或启动 EXE。新增 `tools/war3_native_bootstrap_audit.py`，将四个 native 名称引用、恒成立分支、注册后端及表查找指令逐项核对；固定输入 SHA256，不接受其他版本/不同捕获后仍输出同一结论。机器可读结果见 `analysis/native-bootstrap-20260907/report.json`。原始模块代码不提交。

已确认的链（以下均为这一个游戏构建的 RVA，不是通用偏移）：

- `GroupEnumUnitsSelected`、`GetUnitState`、`GetHeroStr`、`UnitAddAbility` 的名称与 JASS 签名引用处均调用 `0x9dc100` 获取注册分发器，并调用 `0x1fa6ef0` 注册。所传函数地址均为 `0x1fd5620`（返回零的占位函数），因此不能把这些静态 LEA 的目标直接作为可用 native。
- `0x9dc100` 中成对条件跳转及 TEST/OR 后的恒成立跳转最终返回模块内 `0x2aa2a70`。沿入口线性反汇编会误读大量不可达字节；审计工具明确核对实际分支条件。
- 注册包装器实际调用 `0x135f4b0`，逐个访问分发器 `+8`、`+0x10` 两个后端的虚表 `+0x10`。第一后端构造器引用虚表 `0x2449b90`，对应注册方法 `0x13614a0`，再调用 `0x15ac140`。
- 下层注册函数调用 `0x30d360(5)` 获取上下文，以 `context+0x28` 为表，调用 `0x15ad560(table, name)` 查找节点。名称在节点 `+0x28`，函数地址写入 `+0x30`，签名写入 `+0x40`。查找代码使用表掩码、24 字节桶和带标记的链指针，包含 hash 与逐字节名称比较。
- 上下文 getter 使用一个模块全局索引调用导入跳板，再读取返回对象 `+0x38+slot*8`。保存的代码不包含已解析 IAT 数据，尚不能将该导入确定称为 TlsGetValue 或 FlsGetValue。

这改变了后续方向：应在游戏线程中验证“上下文槽 5 → native 表 → 按名称查找”的入口，再取名称/签名一致且非占位的函数。当前外部扫描把 string record 前 8 字节视为 handler，和此处 JASS 表节点布局不同，不能直接复用旧 record 解析器，也不能仅因二者都含同名字符串就认定函数 ABI 相同。

尚未验证当前对局上下文、注册后实际 handler、与现有调用 ABI 的一致性、不同游戏状态/构建的定位。因此正式 helper 和 Python 冷启动路径仍未替换，不能称为冷启动无扫描已完成。本次只运行离线审计工具，没有重复全套回归；应用 1.0.19、协议 32 和已测试 DLL/EXE 均未更改。

复跑（原始 `.text` 捕获仅本机保留）：

```powershell
python tools/war3_native_bootstrap_audit.py --image 'D:\Warcraft III\_retail_\x86_64\Warcraft III.exe' --text analysis/native-bootstrap-20260907/module-text.bin --output analysis/native-bootstrap-20260907/report.json
```

## 还原真实 native 注册与调用入口（2026-09-08）

上一节只追到了静态名称对应的占位注册。本轮发现实际注册函数在栈上通过整数赋值、加减和复制构造名称/签名，所以只搜索明文及 RIP 引用不能发现真实绑定。

新增 `tools/war3_native_registration_extract.py`：以 PE 异常表函数边界和上下文 getter 的直接调用引用筛选候选，在 Unicorn 中模拟构造代码及注册包装器 `0xa15a50` 的真实分支。所有代码均运行于模拟 CPU，代码页只读可执行，只有合成栈可写，安全 cookie 为独立合成页；每个候选恢复初始 CPU 并清空栈。getter 被替换为固定标记，在到达注册分发器 `0x135f4b0` 前提取参数并停止，绝不调用注册后端或 native。未知调用、越界、未映射读取、单候选时间/指令上限均拒绝该候选，冲突绑定不进入可用结果。

结果保存于 `analysis/native-bootstrap-20260907/registrations.json`：1693 个候选，1690 次成功注册，合并一次相同函数/签名的重复注册后为 **1689 个名称**，无冲突。另 3 个候选不满足模拟限制并明确记录原因；不能将它们当作成功，也不能由此声称覆盖整个游戏所有 native。持久快照当前需要的 **29 个名称全部覆盖**。

进一步使用现有 `War3Trainer._native_function_calls` 和 `_discover_agent_resolver`，仅向它们提供离线 `.text` 读取器，验证当前源码可从还原的真实函数推导出：单位句柄解析器 `0xa110b0`、物品句柄解析器 `0xa0dff0`、对象表解析器 `0x31f430`。未实例化正常 trainer、未创建 ProcessMemory、未访问游戏。关键绑定包括 `UnitAddAbility=0xcaaaa0`、`GetUnitState=0xc5bdd0`、`GetItemTypeId=0xc512b0`、`SetUnitPosition=0xca49a0`、`GetHeroStr=0xc50800`。GetUnitState 返回浮点位模式、SetUnitPosition 解引用坐标指针，与当前 helper 的对应声明一致；这不是对所有 native ABI 的实机验证。

后续 DLL 冷启动可以据此核对“模块构建/代码 → 上下文槽 5 → 按名称和签名取真实 handler → 对象解析器”的链，而不必继续依赖私有堆字符串位置。尚需实现此 DLL 入口、拒绝占位/错误上下文、处理游戏状态变化并消除 Python 冷启动对扫描的调用。本轮提交的是可复现逆向证据，没有修改正式协议、DLL 或 EXE。

复跑需 `unicorn`（本机仅安装于未提交的 `_deps`）、`pefile`、`capstone`：

```powershell
$env:PYTHONPATH = (Resolve-Path 'analysis/native-bootstrap-20260907/_deps').Path
python tools/war3_native_registration_extract.py --image 'D:\Warcraft III\_retail_\x86_64\Warcraft III.exe' --text analysis/native-bootstrap-20260907/module-text.bin --output analysis/native-bootstrap-20260907/registrations.json --audit-trainer-bindings
```

## 协议 33：DLL 内 native 表初始化与函数发现（2026-09-08）

已把上述离线证据接入正式源码。新增 `tools/war3_native_bootstrap.h`，在 helper 的游戏线程回调中通过上下文 getter 取得槽 5 的表。使用游戏名称 hash 定位唯一桶，按有符号 link offset、结束标记和逐字节名称比较查找，核对完整签名、构建对应 handler 地址和入口代码指纹。查找设置掩码/指针/4096 步上限，异常捕获后返回错误，没有私有堆扫描或函数占位回退。

`tools/generate_native_bootstrap_profile.py` 根据已核对捕获生成 C 配置和 `war3_native_profile.py` 的相同索引，profile ID 为 `0xdeb8f458`，包含 1689 个名称。当前唯一支持的游戏构建是本机 **2.0.4.23745**（timestamp `0x69e54471`、SizeOfImage `0xdd20000`），不是面向任意游戏更新的通用定位器。地址按当前主模块基址重定位；bootstrap 先核对 PE 架构/版本信息及上下文、hash、三个解析器代码指纹，再调用游戏函数。检查过生成指纹的全部范围，未包含 PE 基址重定位项，ASLR 不会改变这些指纹。未知构建/错误代码明确失败；没有 Windows 版本或堆地址分支。

新增 op 139 初始化：在 DLL 内取得全部 29 个持久 native 后才发布绑定，返回三个解析器和 29 个地址。任一查找失败都不发布部分结果。Python 的 `persistent_native_init` 现在只发这一条命令，校验返回数量与地址后更新会话，不再打开 ProcessMemory 搜索表、分析函数或从 PID 共享缓存恢复。新增 op 140 按编译索引查询额外函数；Python 分批接收成功结果，不接受部分批次。普通、兼容和原 Win10 native discovery 入口统一调用 DLL 查询，删除相应堆发现实现的调用路径。底层旧扫描工具和其他尚未迁移的诊断/组件分析仍保留，不能因此称整个项目已经无扫描。

每条后续 helper 命令执行前，在当前游戏线程重新取得上下文并核对所需的 29 个绑定，避免沿用上一张地图的表对象。它是有界的表查询，不是遍历所有 native 或堆；增加的实际耗时尚未实测。当前用配置中的真实 RVA 严格核对函数，不支持地图/插件替换 native handler 的情形。

离线测试实际编译 C helper，在合成表中验证 hash 碰撞后名称匹配、签名不符、占位/错误地址、代码变化、空表、坏掩码、坏指针、合法负 link offset、循环链及无函数地址的 op 139/140 分发。Python 测试覆盖单命令初始化、旧记录丢弃、失败无回退和额外函数查询；旧普通/备用发现测试按统一 DLL 路径更新。完整结果 **267 passed、75 subtests passed**。协议 33 DLL SHA256：`CD6338FA7FC5693D22267ED75F747DB2E7F65CF0AAB42581310F894FF263E934`。

未连接游戏、未调用实际 native、未启动或打包 EXE，应用版本保持 1.0.19。尚未证明当前真实对局的上下文槽/表满足全部校验，也未覆盖不同对局状态、其他机器或所有字段操作。新链路的实机稳定性、技能实例/物品替换/外部字段迁移仍是未完成项；本次完成的是冷启动及函数发现的源码接入，不能宣告整个目标完成。

## 协议 34：绑定单位的技能元数据查询（2026-09-08）

技能字段页此前分别发送获取技能句柄、ID、等级的命令，再通过 `_ability_instance_from_data_for_candidate` 扫描 owner 附近约 88 MiB 查找指向技能 data 的包装器。即使已有 native 快照，这段扫描仍会运行。新的 op 141 跟随 op 136 单位身份 guard，在同一个游戏线程回调里取得技能句柄、对象、包装器、完整对象身份、效果类、等级和已有数据缓存指针。

离线证据：真实 `BlzGetAbilityId`（RVA `0xc4b7b0`）首先调用 `0xa0b8f0`，成功时读取返回对象 `+0x70`。该能力句柄解析器加入 bootstrap 代码指纹配置；新增范围无 PE ASLR 重定位项。DLL 使用能力 data 的完整身份调用既有对象表解析器，交叉核对 wrapper 的完整身份、所属单位 owner、data 指针，以及 data 的 unit、ID 和镜像 ID；返回前再次核对单位、能力句柄、data 地址和完整代次，损坏或变化即失败。这些 cross-link 偏移来自现有对象模型，当前真实对局是否满足全部约束仍待实机验证。

Python 的 native 技能字段上下文合并为这一条命令，直接消费元数据；按 data 查询包装器时也使用此链，data 不符不扫描找其他对象。普通/兼容 UI 的按单位身份读取技能字段统一恢复绑定快照，不再走旧 Win10 handler recovery 或重新抓当前选择。无可绑定的单位快照时拒绝查询。

新增真实 C 命令分发测试，验证元数据实际通过 IPC 附加结果传回，覆盖缺技能、ID 不符、错误所属单位/包装器、镜像 ID 损坏、对象表缺项、查询中单位或技能代次变化及缺 guard。Python 覆盖英雄/非英雄、单命令上下文、data 不匹配、缺失/畸形元数据、两种 UI 身份入口和失败不扫描。完整离线结果：**281 passed、87 subtests passed**。协议 34 DLL SHA256：`60E5CA2D2626DF706A106041D9895DCD3B3096D2392CF09F8F6D925C1806BC4B`。应用仍为 1.0.19，未连接游戏或打包 EXE。

此处迁移的是技能元数据/效果类查询。技能字段后续读写仍存在跨回调使用能力句柄的链，能力列表包装器枚举、部分英雄技能配置和直接内部效果操作也仍有旧路径；这些不能凭本次测试宣称已完成，需要继续迁移并绑定完整能力身份。

## 协议 35：技能字段跨回调身份绑定（2026-09-08）

`SelectedAbilityFieldContext` 与 UI 的 `AbilityFieldSnapshot` 现在保存能力的 JASS 句柄、data 地址、完整对象身份。旧 UI 快照即使单位、技能 ID 和等级均相同，只要能力代次不符或缺失，就在写前拒绝。字段批次改为单位 guard（136）、能力身份（142）、效果类/等级及元数据查询函数描述（143）加实际字段操作；每批最多 13 个字段。

DLL 对六种技能字段 getter/setter 在执行前后校验单位与能力，能力解析/对象表/所属单位/名称/效果类/等级必须继续符合原绑定。字段函数使用通过校验的能力句柄，命令头保留单位 JASS 句柄，二者不再混用。缺少或位置错误的描述、无 guard 的旧字段操作均拒绝。一个 setter 引起能力或单位身份变化时，立即停止后续操作。

原值读取、写后读回和已有失败恢复均使用同一身份描述；恢复不能绕过校验写到重建后的能力。批量读取在任一命令失败时直接终止，不再把失效上下文下的部分字段发布成完整快照。此处没有承诺事务回滚，无法确认恢复时仍保留禁止继续写入的处理。

真实 C 分发器测试覆盖同批写入/读回、同地址不同代次拒绝、等级不符、缺 guard/描述、setter 后单位/能力/效果类变化。Python 测试实际调用字段读取和写入入口，覆盖旧 UI 快照拒绝、所有后续命令携带同一身份、恢复仍受 guard 限制、分批容量及读取失败不发布快照。完整结果 **289 passed、94 subtests passed**；协议 35 DLL SHA256：`5AC77AB3536BDABA8480F64893EAFC805532D41C18C5727C582D59C8C42B8C5E`。

应用仍为 1.0.19，未连接游戏、未打包 EXE。技能增删/等级快捷操作、直接内部效果、包装器列表枚举及物品字段等其他入口仍需迁移；本次证明范围仅为已接入的技能字段路径及离线模拟，不代表全部游戏状态的实机验证。

## 协议 36：物品字段的槽位与完整身份绑定（2026-09-08）

物品字段上下文现在使用单位绑定快照中的槽位、JASS 物品句柄、对象地址、完整物品代次和 ID，不再分两次回调临时读取槽位/ID。普通与兼容 UI 的单位身份入口统一恢复 native 快照，移除该路径的旧身份/handler recovery 分支。

op 144 携带槽位及完整物品身份，op 145 携带物品 ID，跟随单位身份 guard 136。DLL 在每个物品字段 getter/setter 前后核对当前单位、UnitItemInSlot、物品解析器、对象代次和 GetItemTypeId；实际字段函数使用物品句柄，命令头仍为单位句柄。缺描述、槽位越界、同句柄/同地址但代次不同、类型变化、setter 后单位或物品/槽位变化均拒绝。UI 快照也保存完整物品身份，原值读取、读回和失败恢复始终使用同一个 guard，不能恢复到新物品。

物品字段读取由逐字段回调改为每批最多 13 项，保留目录顺序和未开放项的说明。任一批失败不发布部分字段快照；未开放的字符串传输没有因此被宣称已支持。

真实 C 分发器与 Python 公共入口测试覆盖上述身份/槽位约束、同批写入读回、旧 UI 快照拒绝、恢复受 guard 约束、两种 UI 来源统一、分批数量以及读取失败不发布。完整离线结果 **300 passed、103 subtests passed**；协议 36 DLL SHA256：`53E440AD6B79B3F0B73DFB289FBECFBF4683F814BB06E1F5948A1BD7D53BE712`。

应用仍 1.0.19，未连接游戏、未启动或打包 EXE。背包物品替换、创建/删除/丢弃等快捷操作、技能其他操作及剩余外部字段仍需迁移，实机速度与跨游戏状态稳定性尚未验证。

## 物品槽替换调用约定修正（协议 37，2026-09-08）

离线反汇编确认 `UnitAddItemToSlotById` 的内部槽位函数为 `0x1177560`，调用序列使用单位、物品、槽位和两个标志参数；此前 helper 的函数声明只有四个参数，最后一个参数可能来自未初始化的调用区域。现声明和调用均为五参数，显式传入 `notify=1`、`check_mode=0`，并在创建物品前拒绝大于 5 的槽位。

Python 替换路径改为从 `UnitAddItemToSlotById` 的真实 native handler 提取内部调用，要求调用数量和创建函数交叉校验符合当前构建；不再使用旧的 `UnitAddItem` 偏移猜测。C 测试用带第五参数检查的 fake 函数验证参数，覆盖非法槽位、添加失败以及全量回读。

完整离线结果：**303 passed、103 subtests passed**。协议 37 DLL SHA256：`D681700F584FD9CC506B16DCC2A9B4920228E128FAD5CCF87FC3D20E8FF35FAD`。应用版本保持 1.0.19，未连接游戏、未启动或打包 EXE；该修正仍需实机测试新单位和物品替换速度。


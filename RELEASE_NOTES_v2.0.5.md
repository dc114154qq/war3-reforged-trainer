# Warcraft III Reforged Trainer 2.0.5

适配 Warcraft III `3.0.0.24268`。

## 新增

- 新增独立“3.0 扩展”页，读取和管理 30 格扩展背包与 9 槽装备。
- 扩展背包支持数量修改、复制和丢弃；全部操作绑定物品实例并读回确认。
- 9 槽装备支持安全卸下、套装保存/恢复和槽位类型审计。
- 官方天赋树支持点数显示、增加一点、任意层点亮/替换和洗点，失败自动回滚。
- 老战役角色可启用官方背包框架；没有专属树时可使用不依赖战役脚本的通用六层天赋模板。

## 修复与保护

- 多选只修改生命等非坐标字段时，不再误报必须单选坐标。
- 修复不同设备上 native 入口地址不同导致大象功能预检失败的问题。
- 丢弃扩展背包物品改用同步持有状态接口，避免坐标 native 的 ABI 错误和异步读回。
- 检测到被丢物品承载扩展背包本身时自动放回，避免背包界面失效。
- 复制事务增加精准回滚，新实例数量同步失败时不会留下残留物品。

## 验证

- Warcraft III `3.0.0.24268` 实机完成数量写入恢复、满包保护、九槽套装身份验证、六层洗点恢复、丢弃、复制和副本清理。
- 实机结束后原 30 格背包、9 槽装备和六项天赋均恢复。
- 自动化回归覆盖扩展协议、实例事务、天赋状态、界面翻译及既有功能。

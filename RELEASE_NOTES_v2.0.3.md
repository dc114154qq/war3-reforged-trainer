# Warcraft III Reforged Trainer 2.0.3

适配 Warcraft III `3.0.0.24268`。

## 变更

- 恢复已验证的 `NtMapViewOfSection` 手动映射经典链路，并作为唯一传输路径。
- 通过目标进程实际模块基址解析 API，避免固定系统 DLL 地址导致跨设备失效。
- 增加目标进程权限级别兼容配置，并记录映射、回调、线程和清理状态。
- 失败时保留明确的阶段和 Windows 错误码，不切换到另一套执行方式。
- 应用和文件版本统一更新为 `2.0.3`。

## 验证范围

- 协议和传输层单元回归。
- 冻结包运行自检和独立目录/CWD 可移植性模拟。
- 目标进程实测仍需以实际地图和选中单位状态为准。

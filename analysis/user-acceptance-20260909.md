# 1.0.19 用户验收记录

2026-09-09，用户反馈：“五条我都测了，不仅又快又能用”。

记录为此前交付测试版本的五项操作通过、速度满意。此次反馈没有重新指定
EXE 路径或哈希，不能据此把后续尚未打包的源码改动标为已通过游戏实测。

保留已有测试包：
`dist-v1.0.19-prewarm-machine/魔兽争霸3重制版修改器.exe`。
后续开发保持版本号 1.0.19，使用 `codex/v1.0.19-property-identity` 分支，
不覆盖这个包。本次仅记录反馈并审计源码，没有重新打包或修改游戏状态。

记录时源码 HEAD 为 `3d2df01`。该提交的 619 项测试、103 项子测试通过记录
见 `native-hero-intelligence.md`；这些是已有代码层测试结果，不是本次重新运行。

本次审计确认尚待迁移的具体入口：

- `_write_hero_skill_name_field` 仍在进程外查找英雄组件，读取并写入技能栏
  config/cache，且在写后固定等待 50 ms。运行时技能的替换与技能栏写入
  分属不同步骤，没有共同的游戏线程事务保护。
- `_create_engine_ability_instance` 已有从地图资源创建技能的内部调用；
  后续迁移必须保留这条能力，不能要求其它现存单位先拥有目标技能。

下一步围绕英雄技能栏替换做一项完整迁移：在 DLL 内校验单位和组件身份、
  配置旧值及技能实例，执行创建与栏位更新，并验证失败清理。先做隔离代码
  测试，再决定交付新的独立测试包。上述审计发现不等同于用户已验证操作失败。
